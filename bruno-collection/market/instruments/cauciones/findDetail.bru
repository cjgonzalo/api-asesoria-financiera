meta {
  name: findDetail
  type: http
  seq: 2
}

get {
  url: {{protocol}}://{{url}}:{{port}}/api/{{apiVersion}}/cauciones/cauciones/:instrumentId
  body: none
  auth: inherit
}

params:path {
  instrumentId: {{caucionInstrumentId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
