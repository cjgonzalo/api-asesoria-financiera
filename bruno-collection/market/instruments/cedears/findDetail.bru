meta {
  name: findDetail
  type: http
  seq: 3
}

get {
  url: {{protocol}}://{{url}}:{{port}}/api/{{apiVersion}}/cedears/cedears/:instrumentId
  body: none
  auth: inherit
}

params:path {
  instrumentId: {{cedearInstrumentId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
