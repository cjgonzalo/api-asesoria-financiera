meta {
  name: findCurrentPrices
  type: http
  seq: 5
}

get {
  url: {{protocol}}://{{url}}:{{port}}/api/{{apiVersion}}/cedears/cedears/:instrumentId/current
  body: none
  auth: inherit
}

params:path {
  instrumentId: {{cedearInstrumentId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
