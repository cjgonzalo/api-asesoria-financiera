meta {
  name: findByInstrument
  type: http
  seq: 2
}

get {
  url: {{protocol}}://{{url}}:{{port}}/api/{{apiVersion}}/dollar/dollar/:instrumentId
  body: none
  auth: inherit
}

params:path {
  instrumentId: {{dollarInstrumentId}}
}

settings {
  encodeUrl: true
  timeout: 0
}
